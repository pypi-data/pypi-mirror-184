[tool.poetry]
name = "btcpay-rest-client"
version = "0.0.10"
description = ""
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = ">=3.6,<4"
requests = "^2.26.0"

[tool.poetry.dev-dependencies]
responses = "^0.14.0"
pytest = "^6.2.5"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
