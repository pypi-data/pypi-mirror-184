[project]
name = "wwpy"
description = "World Wide Python"
requires-python = ">=3.10"
license = {text = "BSD 3-Clause License"}
classifiers = [
    "Programming Language :: Python :: 3",
]
dependencies = [
]
dynamic = ["version"]

[tool.setuptools]
packages= ["wwpy"]

[tool.setuptools.dynamic]
version = {attr = "wwpy.__version__"}
