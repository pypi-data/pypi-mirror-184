[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project.scripts]
nbdefense = "nbdefense.main"

[project]
name = "nbdefense"
authors = [{ name = "ProtectAI" }]
readme = "README.md"
license = { file = "LICENSE" }
dynamic = ["version", "description"]
requires-python = "~=3.7"