[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools>=42", "wheel"]

[tool.black]
exclude = "biskit_components/_components|node_modules"
line-length = 79

[tool.pytest.ini_options]
minversion = "6.0"
