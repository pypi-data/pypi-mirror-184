[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "Clarify"
version = "0.5.1"
dependencies = [
    "requests",
    "cssselect",
    "futures",
    "requests-futures",
    "six",
    "wheel"
]
