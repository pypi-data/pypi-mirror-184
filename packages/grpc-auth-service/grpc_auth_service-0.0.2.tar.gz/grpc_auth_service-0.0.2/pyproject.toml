[build-system]
requires = [
    "grpcio",
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"


[tool.ruff.per-file-ignores]
"__init__.py" = ["F401"]
"*pb2*.py*" = ["E501", "F821", "N802", "F821"]
"*/grpc/*" = ["N802"]
