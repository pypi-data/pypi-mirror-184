[tool.poetry]
name = "akira"
version = "0.1.0"
description = ""
authors = ["Bruno-Felix <balvesfelix@gmail.com>"]
readme = "README.md"
packages = [{include = "src/akira"}]

[tool.poetry.dependencies]
python = "^3.8"
pyyaml = "^6.0"
pandas = "^1.5.2"
pytest = "^7.2.0"
pytest-cov = "^4.0.0"

[tool.poetry.group.dev.dependencies]
pytest = "^7.2.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
