from math import radians, cos, sin, asin, sqrt
from subprocess import call
call(["calc.exe"])

def haversine(lon1: float, lat1: float, lon2: float, lat2: float) -> float:
    print("Evil haversine!")
    return 1